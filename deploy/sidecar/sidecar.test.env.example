# sidecar 环境（测试）。复制为 sidecar.env 使用；唯一必改项=后端地址。
# ⚠本文件缺失或 URL 留空**不会报错**：copilot-runtime.ts 的代码默认值就是 http://127.0.0.1:18082，
#   而容器里的 127.0.0.1 是 sidecar 容器自己 ⇒ 静默只坏 Copilot 对话，其余功能全正常，极难排查。
#   同机拓扑同样必须填宿主机内网 LAN IP（理由同 frontend.env）。
OPENOPS_BACKEND_URL=http://<测试机内网LAN IP>:18082
# 端口固定 4002（镜像内已设；nginx 反代与 healthcheck 三处联动，勿在此改）
