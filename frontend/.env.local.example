# ============================================================
# OpenOps 前端环境配置（本机开发 local）
# 复制为 .env.local 并填值，再 `npm run dev` 或 `bash run-frontend.sh`。
# Vite 自动加载 .env.local，不入 git（根 .gitignore 的 .env.* 已排除）。
#
# local 与 test/prod 的差别：关 IAM（走 X-OpenOps-Mock-User/Name 头）、外部系统全 mock。
# 测试/生产环境走公司 IAM Cookie 认证，不需要配置以下变量。
# ============================================================
 
# ---- Mock 登录用户（仅本地开发使用）----
# 前端通过 X-OpenOps-Mock-User/Name 头声明身份；后端首次遇到新 user_id 会自动 upsert。
# 不配则回落默认值 0026demo01 / 林一。
VITE_OPENOPS_DEMO_USER_ID=<你的工号>
VITE_OPENOPS_DEMO_USER_NAME=<你的姓名>
